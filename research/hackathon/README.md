# NEUROMOYO Competition Evidence

This directory contains the frozen benchmark evidence used by the competition-facing product.

- `benchmark/` — CSV results and evaluation manifest
- `figures/` — benchmark figures
- `reproducibility/` — run configuration metadata
- `responsible_ai/` — responsible AI and inclusion note

The raw audio and secrets are intentionally excluded.
